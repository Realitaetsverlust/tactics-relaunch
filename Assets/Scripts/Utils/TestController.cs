using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestController : MonoBehaviour
{
/*    // Start is called before the first frame update
    public void Start() {
        String[] texts = new String[5] {"1", "2", "3", "4", "5"};
        StartCoroutine(this._sayStuff(texts));
    }

    private IEnumerator _sayStuff(String[] texts) {
        foreach(var text in texts) {
            yield return StartCoroutine(this._say(text));
        }
    }

    private IEnumerator _say(String text) {
        while(Input.GetKeyDown(KeyCode.Mouse0) == false) {
            yield return null;
        }
        Debug.Log(text);
        yield return new WaitForSeconds(0.1f);
    }*/
}
